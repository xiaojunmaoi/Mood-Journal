{
  "name": "xinqing-mood-journal",
  "version": "1.0.0",
  "private": true,
  "description": "A warm paper mood journal and self-care HTML prototype",
  "scripts": {
    "dev": "node serve.cjs",
    "test": "node --test tests/*.test.cjs",
    "check": "node --check app.js && node --check journal.js",
    "build": "node build.cjs"
  }
}